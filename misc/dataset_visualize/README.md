## Reasoning Trace Viewer

A web app for exploring recursive reasoning traces stored as JSONL.

### Features
- Collapsible tree explorer
- Graph/DAG visualization
- Metrics (nodes, leaves, depth)
- Node detail inspection

### Usage
1. Open `index.html` in a browser
2. Upload a `.jsonl` file of traces
3. Select a trace and explore

